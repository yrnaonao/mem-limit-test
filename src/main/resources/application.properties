spring.application.name=mem-limit-test
server.port=8080

# Actuator endpoints - only expose health and info for security
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized

# Logging
logging.level.com.example.memlimit=INFO
